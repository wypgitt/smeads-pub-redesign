"use client";

import { SmeadsPubAIChat } from "@/components/contact/smeads-pub-ai-chat";

/** Full-width card variant for /contact (same chat engine as the global drawer). */
export function SmeadsPubAI() {
  return <SmeadsPubAIChat variant="page" />;
}
